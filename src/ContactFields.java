/**
 *  Enum to pass various parameters while searching or removing a contact representing the
 *  fields of a contact object
 */
public enum ContactFields{
    NAME,
    PHONE,
    ADDR,
    EMAIL,
    NOTE
}